[package]
name = "bevy_cloth"
version = "0.1.0"
edition = "2024"

[dependencies]
# fps overlay in the sandbox example
bevy = { version = "0.19.0", features = ["bevy_dev_tools"] }
# same version bevy_math use
glam = "0.32"
rand = "0.10.0"
