use crate::character_controller::camera::IngameCamera;
use bevy::prelude::*;
use bevy_dolly::prelude::*;
use avian3d::prelude::*;

pub(super) fn get_arm_distance(
    camera: &IngameCamera,
    transform: &Transform,
    spatial_query: &SpatialQuery,
    // config: &GameConfig,
) {
}
